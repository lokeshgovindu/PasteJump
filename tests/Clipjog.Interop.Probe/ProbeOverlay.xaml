<Window x:Class="Clipjog.Interop.Probe.ProbeOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Probe Overlay"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        Focusable="False"
        Topmost="True"
        SizeToContent="WidthAndHeight">

    <Border Background="#EE1E1E22" BorderBrush="#FF2563EB" BorderThickness="2" CornerRadius="6" Padding="12">
        <StackPanel>
            <TextBlock Foreground="White" FontWeight="Bold" FontSize="13" Text="Probe overlay" />
            <TextBlock x:Name="DetailText"
                       Foreground="#FF9A9AA6"
                       FontFamily="Consolas"
                       FontSize="11"
                       Margin="0,6,0,0" />
            <TextBlock Foreground="#FF9A9AA6"
                       FontSize="11"
                       TextWrapping="Wrap"
                       MaxWidth="320"
                       Margin="0,6,0,0"
                       Text="This window must never take focus, and clicks must pass through to whatever is underneath." />
        </StackPanel>
    </Border>
</Window>
