<?xml version="1.0" encoding="utf-8"?>
<assembly manifestVersion="1.0" xmlns="urn:schemas-microsoft-com:asm.v1">
  <assemblyIdentity version="1.0.0.0" name="PasteJump.app" />

  <!--
    asInvoker: we do not silently elevate. Note that a non-elevated low-level
    keyboard hook cannot observe keystrokes in elevated windows - the original
    Clipjump nags about running as admin for exactly this reason
    (Clipjump.ahk:158). The clean answer is an optional elevated scheduled task
    at logon rather than baking requireAdministrator in here.
  -->
  <trustInfo xmlns="urn:schemas-microsoft-com:asm.v2">
    <security>
      <requestedPrivileges xmlns="urn:schemas-microsoft-com:asm.v3">
        <requestedExecutionLevel level="asInvoker" uiAccess="false" />
      </requestedPrivileges>
    </security>
  </trustInfo>

  <!--
    Visual styles. Without this dependency the process gets no ComCtl32 v6 activation context, and
    every dialog Windows draws for us - MessageBox above all - falls back to the pre-XP classic style:
    flat square grey buttons, a classic caption, and MS Shell Dlg instead of Segoe UI. It looks like a
    Windows 2000 application and the text is noticeably worse.

    Easy to miss because it is normally invisible. The SDK injects this into the manifest it generates
    for WinForms projects, and WPF's own rendering does not go through ComCtl32 at all - so a WPF app
    looks perfectly modern right up to the first MessageBox. Supplying ApplicationManifest by hand, as
    this project does for dpiAwareness, means supplying this too.
  -->
  <dependency>
    <dependentAssembly>
      <assemblyIdentity type="win32"
                        name="Microsoft.Windows.Common-Controls"
                        version="6.0.0.0"
                        processorArchitecture="*"
                        publicKeyToken="6595b64144ccf1df"
                        language="*" />
    </dependentAssembly>
  </dependency>

  <compatibility xmlns="urn:schemas-microsoft-com:compatibility.v1">
    <application>
      <!-- Windows 10 / 11 -->
      <supportedOS Id="{8e0f7a12-bfb3-4fe8-b9a5-48fd50a15a9a}" />
    </application>
  </compatibility>

  <!--
    PerMonitorV2 is not optional here: the overlay is positioned in physical
    pixels near the caret or cursor, and the foreground window may live on a
    monitor with a different scale factor than the primary display.
  -->
  <application xmlns="urn:schemas-microsoft-com:asm.v3">
    <windowsSettings>
      <dpiAware xmlns="http://schemas.microsoft.com/SMI/2005/WindowsSettings">true/pm</dpiAware>
      <dpiAwareness xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">PerMonitorV2</dpiAwareness>
      <longPathAware xmlns="http://schemas.microsoft.com/SMI/2016/WindowsSettings">true</longPathAware>
    </windowsSettings>
  </application>

</assembly>
